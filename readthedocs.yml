version: 2

python:
   version: "3.12"
   install:
      - method: pip
        path: .
        extra_requirements:
          - dev

build:
    os: ubuntu-22.04
    tools:
      python: "3.12"

sphinx:
    configuration: docs/conf.py
